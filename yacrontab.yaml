defaults:
  environment:
    - key: PYTHONIOENCODING
      value: utf-8

jobs:
  - name: scrapy
    command: scrapy crawl immoscout -o apartments_$(date +"%Y%m%dT%H%M%S").csv -a url=$URL  -L INFO
    schedule: "*/10 * * * *"
    captureStdout: true

